version: '3'
services:

  sasha:
    build: .
    container_name: sasha
    ports:
      - 8000:8000
    volumes:
      - ./src:/sasha/src
      - ./assets:/sasha/assets
    environment:
      SECRET_KEY: ${SECRET_KEY}
